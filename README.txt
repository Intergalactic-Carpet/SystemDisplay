WARNING:

Do not delete any files as these are required to setup the app

Deleting any files will mean that you will have
to re-download the package

RUNNING:

To run, either double click start.bat or the shortcut

Note that the program will run slower with battery saver on

You can adjust settings in settings.ini, if the app uses user
save files, user settings will be found in the saves folder